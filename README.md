# Josie's Site

Simple static site for Cloudflare Pages that provides short paths which redirect to Spotify playlists.

Cloudflare Pages will serve the `public/` directory and honor the `_redirects` file.

- Cloudflare Pages supports Netlify-style `_redirects` files for simple redirect rules.
- The landing page is a placeholder in case Josie wants to do something there.